chocolatey: pacote de instalação, instalou o node e o npm

knex: faz as migrações
npx knex migrate:latest (executa: cria as tabelas)
npx knex migrate:make create_table (cria o arquivo migration referente à tabela [para sua criação])

sqlite: banco utilizado

insomnia: estilo postman, para trabalhar com API, requisições HTTP



routes.get('/users', (request, response) =>{
    // Request, cliente manda pra API (pessoa solicita para ver o conteúdo)
    const body = request.body;
    console.log(body);

    // Response: API manda pro cliente (eu disponibilizo o conteúdo)
    return response.json({
        nome: 'Vinicius',
        idade: 29
    });
});