font awesome
feather icons

BACKEND

chocolatey: pacote de instalação, instalou o node e o npm

knex: faz as migrações
npx knex migrate:latest (executa: cria as tabelas)
npx knex migrate:make create_table (cria o arquivo migration referente à tabela [para sua criação])

sqlite: banco utilizado

insomnia: estilo postman, para trabalhar com API, requisições HTTP



routes.get('/users', (request, response) =>{
    // Request, cliente manda pra API (pessoa solicita para ver o conteúdo)
    const body = request.body;
    console.log(body);

    // Response: API manda pro cliente (eu disponibilizo o conteúdo)
    return response.json({
        nome: 'Vinicius',
        idade: 29
    });
});

===================================================================================================

FRONTEND

iniciar projeto frontend com react: npx create-react-app frontend

componente é uma função que retorna HTML, com funcionalidades CSS, JS...
JSX, quando o HTML tá integrado dentro do JS

Propriedades são como atributos (props) [children pega o que está dentro da tag] parâmetro 'props.children'
{useState}, para alterar valores dentro do react, não posso simplesmente fazer valor += 1, preciso utilizar estados


npm install react-icons
npm install react-router-dom